import XCTest
@testable import StandardHexadecimalColorCode_module

final class StandardHexadecimalColorCode_moduleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(StandardHexadecimalColorCode_module().text, "Hello, World!")
    }
}
